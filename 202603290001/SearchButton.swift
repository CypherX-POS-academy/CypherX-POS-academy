import SwiftUI

struct SearchView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("SearchButton")
        }
        .padding(0)
    }
}

#Preview {
    SearchView()
}
