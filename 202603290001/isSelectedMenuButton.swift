import SwiftUI

struct isSelectedMenuView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedMenuButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedMenuView()
}
