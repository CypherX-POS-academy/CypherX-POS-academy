import SwiftUI

struct ReturnView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("ReturnButton")
        }
        .padding(0)
    }
}

#Preview {
    ReturnView()
}
