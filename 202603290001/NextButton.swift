import SwiftUI

struct NextView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("NextButton")
        }
        .padding(0)
    }
}

#Preview {
    NextView()
}
