import SwiftUI

struct isSelectedReturnView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedReturnButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedReturnView()
}
