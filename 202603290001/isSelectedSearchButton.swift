import SwiftUI

struct isSelectedSearchView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedSearchButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedSearchView()
}
