import SwiftUI

struct ThreeDotView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("ThreeDotButton")
        }
        .padding(0)
    }
}

#Preview {
    ThreeDotView()
}
