import SwiftUI

struct FollowingView: View {
    var body: some View {
        HStack(alignment: .center, spacing: 8) {
            Image("Following")
        }
        .padding(0)
    }
}

#Preview {
    FollowingView()
}
