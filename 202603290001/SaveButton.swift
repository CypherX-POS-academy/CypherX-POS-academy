import SwiftUI

struct SaveView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("SaveButton")
        }
        .padding(0)
    }
}

#Preview {
    SaveView()
}
