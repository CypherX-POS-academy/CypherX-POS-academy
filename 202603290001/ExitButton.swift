import SwiftUI

struct ExitView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
        Image("ExitButton")
        }
        .padding(0)
    }
}

#Preview {
    ExitView()
}
