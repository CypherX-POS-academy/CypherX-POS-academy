import SwiftUI

struct InfoView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("InfoButton")
        }
        .padding(0)
    }
}

#Preview {
    InfoView()
}
