import SwiftUI

struct isSelectedThreeDotView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedThreeDotButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedThreeDotView()
}
