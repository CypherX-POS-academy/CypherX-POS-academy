import SwiftUI

struct isSelectedSaveView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedSaveButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedSaveView()
}
