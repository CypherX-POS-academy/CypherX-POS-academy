import SwiftUI

struct MenuView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("MenuButton")
        }
        .padding(0)
    }
}

#Preview {
    MenuView()
}
