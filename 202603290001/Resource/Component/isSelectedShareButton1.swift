import SwiftUI

struct isSelectedShareView: View {
    var body: some View {
        VStack {
            Image("isSelectedShareButton1")
              .frame(width: 9, height: 9)
        }
    }
}

#Preview {
    isSelectedShareView()
}
