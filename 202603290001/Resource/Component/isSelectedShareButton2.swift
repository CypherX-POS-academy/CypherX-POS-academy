import SwiftUI

struct isSelectedShare2View: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedShareButton2")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedShare2View()
}
