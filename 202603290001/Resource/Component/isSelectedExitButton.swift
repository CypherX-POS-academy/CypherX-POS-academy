import SwiftUI

struct isSelectedExitView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
        Image("isSelectedExitButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedExitView()
}
