import SwiftUI

struct isSelectedPlusView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedPlusButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedPlusView()
}
