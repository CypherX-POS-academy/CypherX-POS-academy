import SwiftUI

struct isSelectedInfoView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedInfoButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedInfoView()
}
