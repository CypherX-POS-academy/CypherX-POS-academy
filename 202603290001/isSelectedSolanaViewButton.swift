
import SwiftUI

struct isSelectedSolanaView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Image("isSelectedSolanaViewButton")
        }
        .padding(.horizontal, 0)
        .padding(.top, 12)
        .padding(.bottom, 0)
        .cornerRadius(12)
    }
}

#Preview {
    isSelectedSolanaView()
}
