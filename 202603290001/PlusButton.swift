import SwiftUI

struct PlusView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("PlusButton")
        }
        .padding(0)
    }
}

#Preview {
    PlusView()
}
