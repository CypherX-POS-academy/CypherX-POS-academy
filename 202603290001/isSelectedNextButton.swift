import SwiftUI

struct isSelectedNextView: View {
    var body: some View {
        VStack(alignment: .center, spacing: 0) {
            Image("isSelectedNextButton")
        }
        .padding(0)
    }
}

#Preview {
    isSelectedNextView()
}
